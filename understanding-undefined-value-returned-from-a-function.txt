// Налаштування
let sum = 0;

function addThree() {
  sum = sum + 3;
}

// Змініть код лише під цим рядком
function addFive(num) {
  sum = sum + 5;
}

// Змініть код лише над цим рядком

addThree();
addFive();